# YOLO 螢幕截圖裁剪工具 - 規格說明文檔

## 1. 項目概述

**項目名稱**: YOLO Screenshot Cropping Tool
**項目類型**: Windows 桌面應用程式
**核心功能**: 支援螢幕截圖、剪貼簿貼上、檔案拖放等方式載入圖片，並透過可移動、可調整大小的裁剪框進行精確裁剪，輸出符合 YOLO 格式需求的圖片。額外支援影片模式，可逐幀瀏覽影片並進行裁剪。
**目標用戶**: 需要批量裁剪圖片用於 YOLO 目標檢測模型訓練的使用者

---

## 2. UI/UX 規格

### 2.1 視窗佈局

- **主視窗**: 單一視窗應用，標準標題列（含最小化、最大化、關閉按鈕）
- **視窗尺寸**: 預設 1200x800，可調整大小，最小尺寸 800x600
- **佈局分區**:
  - **頂部工具列** (高度 50px): 功能按鈕和參數輸入
  - **中央畫布區域** (彈性填充): 圖片顯示與裁剪框操作
  - **底部狀態列** (高度 30px): 顯示當前狀態和提示資訊

### 2.2 視覺設計

**色彩方案**:
- 主色: `#1F6AA5` (深藍色)
- 次色: `#3B8ED0` (亮藍色)
- 強調色: `#2CC985` (綠色，用於確認/保存)
- 背景色: `#1A1A1A` (深灰色)
- 表面色: `#2D2D2D` (中等灰色)
- 文字色: `#FFFFFF` (白色)
- 次要文字: `#A0A0A0` (灰色)

**裁剪框樣式**:
- 裁剪框邊框: 2px 實線，顏色 `#FF6B35` (橙色)
- 裁剪框四角: 8x8px 正方形控制點，顏色 `#FF6B35`
- 遮罩層: 半透明黑色 `rgba(0, 0, 0, 0.6)`
- 裁剪框內部: 完全透明（顯示原圖）

**字體**:
- 主字體: Microsoft JhengHei UI, 12px
- 標題字體: Microsoft JhengHei UI Bold, 14px

**間距**:
- 工具列內邊距: 10px
- 按鈕間距: 8px
- 輸入框高度: 32px

### 2.3 元件清單

**頂部工具列**:
1. **載入按鈕** (打開檔案對話框)
2. **從剪貼簿貼上按鈙** (Ctrl+V 快捷鍵)
3. **開啟影片按鈕** (支援 MP4, AVI, MOV, MKV, WMV, FLV, WebM)
4. **裁剪寬度輸入框** + 標籤 "寬度:"
5. **裁剪高度輸入框** + 標籤 "高度:"
6. **輸出路徑輸入框** + 瀏覽按鈕
7. **保存按鈙** (Enter 快捷鍵)
8. **重置按鈙**

**影片控制區域** (影片模式時顯示):
1. **上一幀按鈕** (◀◀)
2. **播放/暫停按鈕** (▶ / ⏸)
3. **下一幀按鈕** (▶▶)
4. **幀數顯示標籤** (第 X / Y 幀 | Z FPS)

**中央畫布區域**:
- 圖片顯示區域（自適應縮放）
- 裁剪框（可拖曳移動）
- 四角調整手柄（調整大小）
- 遮罩層

**底部狀態列**:
- 左側: 當前操作提示
- 右側: 圖片尺寸資訊

---

## 3. 功能規格

### 3.1 核心功能

**圖片載入方式**:
1. **檔案對話框**: 點擊"開啟檔案"按鈕，支援 PNG、JPG、JPEG、BMP、GIF、WebP 格式
2. **剪貼簿貼上**: 使用 Ctrl+V 或點擊按鈕從剪貼簿讀取圖片
3. **拖放**: 將圖片檔案拖入應用程式視窗
4. **剪貼簿路徑**: 支援從剪貼簿貼上圖片檔案路徑（自動偵測並載入）

**影片模式**:
1. **影片開啟**: 點擊"開啟影片"按鈕，支援 MP4、AVI、MOV、MKV、WMV、FLV、WebM 格式
2. **影片拖放**: 可直接將影片檔案拖入應用程式視窗
3. **播放控制**: 播放/暫停按鈕，切換影片播放狀態
4. **逐幀導航**: 上一幀(◀◀)、下一幀(▶▶) 按鈕
5. **幀數顯示**: 即時顯示目前幀數/總幀數及 FPS
6. **裁剪保存**: 裁剪框可直接套用於目前影片幀，保存時自動命名為 `影片名_frame_XXXX.png`
7. **快捷鍵**:
   - 左方向鍵: 上一幀
   - 右方向鍵: 下一幀
   - 空白鍵: 播放/暫停

**取消聚焦**:
- 點擊空白處可取消輸入欄位的聚焦，確保剪貼簿貼上功能正常運作

**裁剪框操作**:
1. **移動**: 滑鼠左鍵點擊並拖曳裁剪框內部
2. **調整大小**: 拖曳裁剪框四角的調整手柄
3. **約束**: 裁剪框最小尺寸為 50x50，最大不得超過圖片尺寸
4. **初始位置**: 圖片載入後，裁剪框置於圖片中央
5. **初始尺寸**: 預設 640x640，或使用參數輸入框中的值

**參數調整**:
1. 寬度輸入: 範圍 50-原圖寬度，預設 640
2. 高度輸入: 範圍 50-原圖高度，預設 640
3. 參數修改後，裁剪框即時更新尺寸

**圖片保存**:
1. 點擊"保存"按鈕或按 Enter 鍵
2. 裁剪框範圍內的圖片進行裁剪
3. 輸出路徑: 使用者指定的目錄
4. 檔案名稱: 沿用原檔案名稱（不含路徑）
5. 圖片格式: 保持原格式，或可選擇轉換
6. **資料夾模式**: 可開啟包含多張圖片的資料夾，使用左右方向鍵瀏覽圖片

### 3.2 使用者互動流程

1. 啟動應用程式 → 顯示空白畫布，提示"請載入圖片"
2. 載入圖片 → 圖片顯示於中央，裁剪框置於中央，遮罩層覆蓋
3. 調整裁剪框 → 移動位置或調整大小
4. 輸入/調整參數 → 裁剪框尺寸即時更新
5. 設定輸出路徑 → 點擊瀏覽或手動輸入
6. 保存裁剪 → 裁剪並保存圖片，顯示成功訊息

### 3.3 邊界情況處理

- **未載入圖片時**: 保存按鈕禁用，顯示提示
- **裁剪框超出邊界**: 自動約束到圖片邊界內
- **輸出路徑無效**: 顯示錯誤訊息，提示重新選擇
- **檔案已存在**: 詢問是否覆蓋
- **剪貼簿無圖片**: 顯示提示"剪貼簿中無圖片"

---

## 4. 技術實現

### 4.1 技術堆疊

- **語言**: Python 3.8+
- **GUI 框架**: Tkinter (內建) + Pillow (影像處理)
- **圖形處理**: Pillow (PIL)
- **影片處理**: OpenCV (opencv-python)
- **拖放支援**: tkinterdnd2

### 4.2 模組結構

```
yolo_cropper/
├── main.py              # 應用程式入口
├── image_canvas.py      # 圖片顯示與裁剪框元件
├── crop_algorithm.py    # 裁剪算法
└── utils.py             # 工具函數
```

---

## 5. 驗收標準

### 5.1 功能驗收

- [x] 可透過檔案對話框開啟圖片
- [x] 可從剪貼簿貼上圖片 (Ctrl+V)
- [x] 可拖放圖片檔案到應用程式
- [x] 裁剪框可自由移動
- [x] 裁剪框可透過四角調整大小
- [x] 寬度/高度參數可輸入並即時更新裁剪框
- [x] 預設裁剪框為 640x640
- [x] 可設定輸出路徑
- [x] 保存功能正常，圖片裁剪正確
- [x] 檔案名稱沿用原名稱
- [x] 支援從剪貼簿貼上圖片路徑
- [x] 支援開啟資料夾並瀏覽多張圖片
- [x] 可透過按鈕開啟影片檔案 (MP4, AVI, MOV, MKV, WMV, FLV, WebM)
- [x] 可拖放影片檔案到應用程式
- [x] 影片播放/暫停功能正常
- [x] 逐幀導航功能正常 (上一幀/下一幀)
- [x] 幀數顯示正確
- [x] 影片裁剪保存時自動命名為 `影片名_frame_XXXX.png`
- [x] 方向鍵可控制影片幀導航 (左:上一幀, 右:下一幀)
- [x] 空白鍵可切換播放/暫停

### 5.2 視覺驗收

- [ ] 深色主題 UI
- [ ] 裁剪框有明顯的橙色邊框
- [ ] 遮罩層正確覆蓋非裁剪區域
- [ ] 裁剪框內部區域清晰可見
- [ ] 調整手柄視覺明顯

### 5.3 互動驗收

- [x] Enter 鍵可觸發保存
- [x] 裁剪框移動流暢
- [x] 參數輸入後裁剪框即時響應
- [x] 狀態列顯示相關提示資訊
- [x] Ctrl+V 貼上功能正常運作
- [x] 點擊空白處可取消輸入欄位聚焦
